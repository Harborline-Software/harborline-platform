# Harborline Blocks Builder Definitions

Local-shadow .NET package for the narrow substrate shared by Forms and Workflows builders: advisory key suggestion and a tenant-scoped durable archive lifecycle. `IDefinitionKeyAuthority` is intentionally unimplemented until allocation authority is ruled. This package owns no authored definition payload, transport route, Pilot bridge, or UI.
