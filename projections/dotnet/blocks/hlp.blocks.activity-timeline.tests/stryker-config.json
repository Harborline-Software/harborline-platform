{
  "$schema": "https://json.schemastore.org/stryker-config",
  "stryker-config": {
    "project": "Harborline.Blocks.ActivityTimeline.csproj",
    "since": { "enabled": true, "target": "origin/main" },
    "thresholds": { "high": 80, "low": 60, "break": 60 },
    "reporters": ["json"]
  }
}
