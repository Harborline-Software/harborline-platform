# Harborline Platform

This private gallery renders the packaged Blazor projection of each reusable UI module in isolation.
The Button stories are sourced from the neutral `hlp.ui.button` gallery scenario catalog and are
verified against the React projection before the Platform gate passes.
