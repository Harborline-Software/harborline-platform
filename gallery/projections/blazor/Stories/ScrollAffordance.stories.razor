@attribute [Stories("Platform/Scroll Affordance")]
<Stories TComponent="ScrollAffordanceScenario">@foreach (var item in Items) { <Story Name="@item.Name"><Arguments><Arg For="_ => _.ScenarioId" Value="@item.Id" /></Arguments><Template><ScrollAffordanceScenario @attributes="context.Args" /></Template></Story> }</Stories>
@code { private static readonly (string Id,string Name)[] Items=[("scroll-affordance.axes-overflow","Axes and overflow state"),("scroll-affordance.keyboard-status","Keyboard and live position"),("scroll-affordance.snap-fade","Snap and logical fades"),("scroll-affordance.replacement-host","Host attributes and replacement"),("scroll-affordance.locale-pseudo","Pseudo-localized announcements"),("scroll-affordance.locale-ar","Arabic logical scrolling"),("scroll-affordance.theme-light","Light theme"),("scroll-affordance.theme-dark","Dark theme")]; }
