@attribute [Stories("Platform/Scheduler")]
<Stories TComponent="SchedulerScenario" Layout="@typeof(NoMarginLayout)">@foreach(var item in Items){<Story Name="@item.Name"><Arguments><Arg For="_ => _.ScenarioId" Value="@item.Id" /></Arguments><Template><SchedulerScenario @attributes="context.Args" /></Template></Story>}</Stories>
@code { private static readonly (string Id,string Name)[] Items=[("scheduler.views-state","Views and controlled state"),("scheduler.navigation-layout","Navigation and calendar layout"),("scheduler.month-agenda","Month drilldown and agenda"),("scheduler.narrow-reflow","Narrow responsive schedule"),("scheduler.now-readonly","Current time and read-only policy"),("scheduler.editing-recurrence","Editing and recurrence workflow"),("scheduler.large-data","Bounded large schedule"),("scheduler.locale-pseudo","Pseudo-localized schedule"),("scheduler.locale-ar","Arabic date, time, and direction"),("scheduler.theme-light","Light theme"),("scheduler.theme-dark","Dark theme")]; }
