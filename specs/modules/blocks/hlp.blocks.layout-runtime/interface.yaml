{"schemaVersion":1,"moduleId":"hlp.blocks.layout-runtime","interfaceRevision":1,"semantics":{"operation":"resolve one immutable Layout definition binding and lower its admitted node tree into a projection-neutral flow plan","binding":"a surface supplies both its definition id and immutable version id; resolution refuses absent, malformed, or unpublished bodies","flow":"screen flow preserves tree order; page flow separates static regions and preserves explicit break boundaries","ownership":"the runtime does not persist definitions, mint versions, fetch data, or render framework markup"},"dependencies":["hlp.blocks.builder-definitions"],"invariants":["Runtime admission rejects malformed persisted values before planning.","The resolver binds exactly the caller-supplied immutable definition version.","Unknown layout block kinds are not converted into fallback content.","The flow planner preserves declared order and static-region identity."]}
